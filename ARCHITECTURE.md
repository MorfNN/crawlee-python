# 🏗️ Архитектура системы Gemini Automation

## 📋 Общая структура

```
┌─────────────────────────────────────────────────────────────┐
│                    ВЕББРАУЗЕР ПОЛЬЗОВАТЕЛЯ                  │
│              http://localhost:8000                          │
│  ┌────────────────────────────────────────────────────────┐ │
│  │              ВЕБИ-ИНТЕРФЕЙС (index.html)               │ │
│  │  ┌─────────────┐  ┌──────────────┐  ┌──────────────┐  │ │
│  │  │ Задача 1    │  │  Задача 2    │  │  + Добавить  │  │ │
│  │  │ 50 сессий   │  │  30 сессий   │  │  задачу      │  │ │
│  │  │ ▶ прогресс  │  │  ▶ прогресс  │  │              │  │ │
│  │  └─────────────┘  └──────────────┘  └──────────────┘  │ │
│  └────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
                           ▲
                           │ HTTP + WebSocket
                           ▼
┌─────────────────────────────────────────────────────────────┐
│                     PYTHON ПРИЛОЖЕНИЕ                       │
│               gemini_automation_extended.py                 │
│  ┌────────────────────────────────────────────────────────┐ │
│  │ FastAPI приложение (uvicorn)                           │ │
│  │ ┌──────────────────────────────────────────────────┐   │ │
│  │ │  /api/tasks          - Manage tasks             │   │ │
│  │ │  /api/tasks/start    - Run task                 │   │ │
│  │ │  /api/tasks/stop     - Stop task                │   │ │
│  │ │  /ws                 - WebSocket updates        │   │ │
│  │ └──────────────────────────────────────────────────┘   │ │
│  └────────────────────────────────────────────────────────┘ │
│                           │
│          ┌────────────────┼────────────────┐
│          ▼                ▼                ▼
│  ┌────────────────┐ ┌────────────────┐ ┌──────────┐
│  │ Task Manager   │ │ Session Manager│ │ Browser  │
│  │                │ │                │ │ Instance │
│  │ ┌─────────────┐│ │┌──────────────┐│ │          │
│  │ │Task 1       ││ ││ Волна 1      ││ │Chromium  │
│  │ │  status     ││ ││  Сессия 1    ││ │+ Pages   │
│  │ │  sessions   ││ ││  Сессия 2    ││ │          │
│  │ │  results    ││ ││ Волна 2      ││ │          │
│  │ └─────────────┘│ ││  Сессия 3    ││ │          │
│  │ ┌─────────────┐│ │└──────────────┘│ │          │
│  │ │Task 2       ││ │┌──────────────┐│ │          │
│  │ │  ...        ││ ││...           ││ │          │
│  │ └─────────────┘│ │└──────────────┘│ │          │
│  └────────────────┘ └────────────────┘ └──────────┘
└─────────────────────────────────────────────────────────────┘
                           │
                           │ Playwright API
                           ▼
┌─────────────────────────────────────────────────────────────┐
│                  БРАУЗЕР CHROMIUM                           │
│                  (headless=False)                           │
│              https://gemini.browserbase.com/                │
│  ┌─────────────────────────────────────────────────────┐    │
│  │  Сессия   │  Сессия   │  Сессия   │  Сессия       │    │
│  │     1     │     2     │     3     │     4         │    │
│  │           │           │           │               │    │
│  │ - Type    │ - Type    │ - Type    │ - Type        │    │
│  │ - Run     │ - Run     │ - Run     │ - Run         │    │
│  │ - Allow   │ - Allow   │ - Allow   │ - Allow       │    │
│  │ - Restart │ - Restart │ - Restart │ - Restart     │    │
│  └─────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────┘
                           ▲
                           │ Control
                           │
                         Gemini Browser
```

---

## 🔄 Жизненный Цикл Одной Сессии

```
┌─────────────────────────────────────────────────────────┐
│ СЕССИЯ СОЗДАНА                                          │
│ - task_id: task_123                                     │
│ - session_id: 1                                         │
│ - prompt: "Сколько стоит акция NVIDIA?"                │
└────────────────┬────────────────────────────────────────┘
                 │
                 ▼
        ОЖИДАНИЕ (волна)
        (random 0-15 сек)
                 │
                 ▼
     ┌──────────────────────┐
     │  СОЗДАНИЕ КОНТЕКСТА  │
     │  - new_context()     │
     │  - new_page()        │
     │  - скрыть WebDriver  │
     └──────────┬───────────┘
                │
                ▼
     ┌──────────────────────┐
     │ ЗАГРУЗКА САЙТА       │
     │ goto(URL)            │
     │ waitForLoadState()   │
     └──────────┬───────────┘
                │
                ▼
     ┌──────────────────────┐
     │ ПОИСК ПОЛЯ ВВОДА     │
     │ querySelector()      │
     │ wait_for_selector()  │
     └──────────┬───────────┘
                │
                ▼
     ┌──────────────────────┐
     │ ВВОД ТЕКСТА          │
     │ click field          │
     │ sleep(0.5-1.5s)      │
     │ keyboard.type(char)  │ ← Печатает по одному символу
     │ sleep(0.05-0.15s)    │   с случайными задержками
     │ ... повторить ...    │
     └──────────┬───────────┘
                │
                ▼
     ┌──────────────────────┐
     │ НАЖАТИЕ RUN          │
     │ click(run_button)    │
     │ sleep(0.5-1.5s)      │
     └──────────┬───────────┘
                │
                ▼
     ┌──────────────────────┐ Запускается параллельно
     │ МОНИТОРИНГ ALLOW     │ (asyncio.create_task)
     │ Каждую 1 сек:        │
     │ - поиск Allow        │
     │ - нажатие Allow      │
     │ - sleep(0.3-0.8s)    │
     │ - повтор...          │
     └──────────┬───────────┘
                │
                ▼
        ☕ ОЖИДАНИЕ 5 МИНУТ
        (session_timeout = 330)
                │
                ▼
     ┌──────────────────────┐
     │ ПОИСК RESTART        │
     │ wait_for_selector()  │
     │ timeout = 60 сек     │
     └──────────┬───────────┘
                │
                ▼
     ┌──────────────────────┐
     │ НАЖАТИЕ RESTART      │
     │ click(restart_btn)   │
     │ sleep(2)             │
     └──────────┬───────────┘
                │
                ▼
        ✅ СЕССИЯ ЗАВЕРШЕНА
           (iteration++)
                │
        ┌───────┴───────┐
        │               │
      Повторить  Отменить задача
      (до 5х)     (status != "running")
        │               │
        ▼               ▼
      НОВАЯ           ЗАКРЫТЬ
      ИТЕРАЦИЯ        БРАУЗЕР
```

---

## 🌊 Волнообразное распределение (Ключевая Фишка)

### Без волн (❌ Выглядит как бот):

```
Время →
00:00  ████████████████████  100% (все сразу)
00:01  ░░░░░░░░░░░░░░░░░░░░  0%

Проблема: Все браузеры запустились одновременно
Результат: IP банят за 2 минуты
```

### С волнами (✅ Выглядит как реальные пользователи):

```
              Волна 1    Волна 2    Волна 3
Время ↓
00:00 █████     (5 сеш)
:01   ░░░░░░░░░░░░░░░░░
:02   ░░░░░░░░░░░░░░░░░
:03            █████      (5 сеш)
:04            ░░░░░░░░░░░░░░░░░
:05            ░░░░░░░░░░░░░░░░░
:06                       █████     (остаток)
:07                       ░░░░░░░░░░░░░░░░░
```

**Эффект:**
- Не 20 браузеров одновременно (подозрительно)
- А 5 браузеров → 45 сек паузы → 5 браузеров (естественно)
- Выглядит как разные люди заходили в разное время

---

## 🔁 Жизненный Цикл Задачи

```
СОЗДАНИЕ
  │
  ├─ Task создается с ID
  ├─ Статус: "pending"
  ├─ Сохраняется в памяти
  └─ Отправляется WebSocket клиентам
         │
         ▼
    ЗАПУСК
      │
      ├─ Статус: "running"
      ├─ Волна 1 стартует
      │  ├─ Сессия 1 ├─ Сессия 2 ├─ ... ├─ Сессия N
      │  │ (delay)  │ (delay)  │      │ (delay)
      │  └─────────────────────────────┘
      │            ↓
      │   ОЖИДАНИЕ 45-75 сек
      │            │
      ├─ Волна 2 стартует
      │
      ├─ Волна 3 стартует
      │
      ├─ Волна N стартует
      │
      ├─ ... сессии работают ...
      │
      └─ Все волны завершены
         │
         ▼
    ЗАВЕРШЕНИЕ
      │
      ├─ Статус: "completed"
      ├─ Результаты сохранены в JSON
      ├─ Логи записаны
      └─ WebSocket уведомление отправлено
         ├─ Веб-интерфейс обновляет UI
         └─ Progress bar показывает 100%
```

---

## 📊 Состояния Задачи

```
┌─────────────┐
│   pending   │  (Создана, но не запущена)
└──────┬──────┘             ▲
       │ start()            │
       ▼                    │ stop()
┌──────────────┐            │
│   running    │────────────┘
└──────┬───────┘
       │ (все волны OK)    (ошибка)
       │                  ∙∙∙∙∙∙∙
       ▼                  │
┌────────────────┐  ┌──────────────┐
│   completed    │  │   stopped    │
└────────────────┘  └──────────────┘
```

---

## 💾 Сохранение Данных

```
DURING EXECUTION
  │
  ├─ Каждая сессия сохраняет результат в памяти
  │  task.save_result(session_id, success, data)
  │
  ├─ Каждые 10 сессий сохраняется в файл
  │  task.save_to_file()
  │
  └─ Каждые 2 сек обновляется WebSocket
     ws.send_json({type: 'task_update', task: {...}})
        │
        ▼
   ВЕБ-ИНТЕРФЕЙС
   (обновляет статус в браузере)

AFTER EXECUTION
  │
  ├─ После завершения (или остановки):
  │  task.save_to_file()
  │
  ├─ Сохраняется в:
  │  results/task_123456_results.json
  │
  └─ Содержит:
     ├─ task_id
     ├─ prompt
     ├─ session_count
     ├─ completed_sessions
     ├─ failed_sessions
     └─ results[]
        ├─ session_id
        ├─ timestamp
        ├─ success: true/false
        └─ data: {...}
```

---

## 🔐 Анти-бан Слои

```
СЛОЙ 1: БРАУЗЕРНЫЙ
├─ User-Agent реальный
├─ WebDriver скрыт
└─ Cookies/LocalStorage

СЛОЙ 2: ПОВЕДЕНИЕ
├─ Случайные задержки (0.5-1.5 сек)
├─ Случайная скорость печати (0.05-0.15 сек/символ)
├─ Случайные интервалы нажатий
└─ Вариабельность во всём

СЛОЙ 3: РАСПРЕДЕЛЕНИЕ НАГРУЗКИ
├─ Волнообразный запуск (не все сразу)
├─ Задержки между волнами (45-75 сек)
├─ Случайные задержки в волне (0-15 сек)
└─ Имитация натурального приема пользователей

СЛОЙ 4: СЕТЕВОЙ (опционально)
├─ VPN / Прокси
├─ Residential IP
├─ Ротация IP адресов
└─ Разные браузеры на разных IP
```

**Результат:** Сложнейшим фреймворкам трудно определить что это бот

---

## 📈 Масштабирование

### 1-100 сессий: Single Server

```
┌─────────────────────┐
│  Один Python процесс│
│  (gemini_automation)│
│                     │
│ ├─ 1-20 браузеров   │
│ ├─ 1 волна          │
│ ├─ 4GB RAM          │
│ └─ $10-40/месяц     │
└─────────────────────┘
```

### 100-500 сессий: Medium Server

```
┌──────────────────────┐
│  Один Python процесс │
│  на более мощном ПК  │
│                      │
│ ├─ 20-50 браузеров   │
│ ├─ 10-20 волн        │
│ ├─ 8-16GB RAM        │
│ └─ $40-100/месяц     │
└──────────────────────┘
```

### 500-5000 сессий: Distributed

```
┌────────────────────────────────────┐
│        LOAD BALANCER               │
│        (Nginx / HAProxy)           │
└─────────┬──────────────────┬──────┘
          │                  │
     ┌─────────┐        ┌─────────┐
     │Server 1 │        │Server 2 │
     │  1000   │        │  1000   │
     │sessions │        │sessions │
     │  8GB    │        │  8GB    │
     └─────────┘        └─────────┘
          │                  │
          ├─────────┬────────┘
          │         │
    ┌─────────┐ ┌─────────┐
    │Server 3 │ │Server 4 │
    │  1000   │ │  1000   │
    │sessions │ │sessions │
    │  8GB    │ │  8GB    │
    └─────────┘ └─────────┘

Total: 4000+ сессий
Cost: ~$100-150/месяц
```

### 5000+ сессий: Enterprise

```
KUBERNETES КЛАСТЕР
├─ Auto-scaling (5-100 pods)
├─ Каждый pod: 100-500 сессий
├─ Load balancing
├─ Health checks
├─ 99.9% uptime
├─ Residential прокси
└─ Cost: ДОРОГО (>$500/месяц)
```

---

## 🎛️ Управление и Мониторинг

```
WEB INTERFACE              API              LOGS              FILES
(index.html)          (FastAPI)      (gemini_automation.)  (results/)
    │                    │                    │                 │
    ├─ View tasks        ├─ POST /api/tasks   ├─ 09:00:00 🤖 start  ├─ task_123_results.json
    ├─ Create task       ├─ GET /api/tasks    ├─ 09:00:10 🌊 wave   ├─ task_124_results.json
    ├─ Start task        ├─ POST /start       ├─ 09:00:20 ✅ allow  ├─ task_125_results.json
    ├─ Stop task         ├─ POST /stop        ├─ 09:05:00 ✅ done   └─ ...
    ├─ Real-time stats   ├─ WebSocket /ws     ├─ 09:10:00 📊 summary
    └─ Progress bars     └─ GET /health       └─ 09:15:00 💾 saved
```

---

## 🧠 Умная Логика

### 1. Выбор режима волн

```python
if sessions <= 100:
    # Маленькие волны (осторожно)
    sessions_per_wave = 3
    delay = 60 сек
elif sessions <= 500:
    # Средние волны
    sessions_per_wave = 8
    delay = 45 сек
else:
    # Крупные волны
    sessions_per_wave = 15
    delay = 30 сек
```

### 2. Распределение в волне

```python
# Каждую сессию в волне запускаем с случайной задержкой
for i in range(sessions_in_wave):
    initial_delay = random.uniform(0, 15)  # 0-15 сек
    create_session(..., delay=initial_delay)
```

### 3. Обработка ошибок

```python
try:
    # Попытка выполнить сессию
    await create_session(...)
except TimeoutError:
    # Timeout? Отметить как ошибку, продолжить
    failed_sessions += 1
except Exception as e:
    # Другая ошибка? Залогировать, продолжить
    logger.error(f"Error: {e}")
```

### 4. Graceful shutdown

```python
# stop_task() отмучает все сессии текущей задачи
# но их результаты сохраняются в файл
# вы можете запустить снова позже
```

---

## ⏰ Тайминги (Типичные)

```
Одна сессия (итерация):
├─ Загрузка сайта:        5-10 сек
├─ Ввод текста:           10-20 сек (с задержками)
├─ Нажатие Run:           1-2 сек
├─ Allow монитор:         5 минут (passive wait)
├─ Поиск Restart:         5-60 сек
└─ Нажатие Restart:       2 сек
   ───────────────────────────
   TOTAL:                  ~5-6 минут

Волна (5 сессий):
├─ Запуск 5 сессий:       0-15 сек (распределено)
├─ Работа:                ~5-6 минут каждая
├─ Завершение:            ~30 сек
└─ TOTAL:                 ~6 минут

Задача (5 волн × 5 сессий = 25 всего):
├─ Волна 1:               ~6 минут
├─ Пауза между волнами:   ~60 сек
├─ Волна 2-5:             ~24 минут
├─ Завершение:            ~2 минуты
└─ TOTAL:                 ~32 минуты
```

---

**Архитектура разработана для масштабируемости, надежности и защиты от обнаружения! 🚀**
