# 🛡️ Защита от Банов и Antifrand - Полное руководство

## Понимание проблемы

Browserbase и Gemini используют различные механизмы для отслеживания ботов:

1. **Паттерны поведения** - одновременные запросы, идентичные интервалы
2. **IP блокировка** - множественные подключения с одного IP
3. **Браузер сигналы** - user-agent, WebDriver флаги
4. **Активность паттерны** - неестественное распределение запросов

---

## ✅ Встроенные Анти-бан Механизмы

### 1. Волнообразное распределение сессий

**Как это работает:**

```
Реальные пользователи (естественный паттерн):
09:00 - 1 пользователь
09:15 - 3 пользователя
09:30 - 2 пользователя  
...

Наша система (волны):
Wave 1 (09:00):  3-5 сессий   ─┐
                                 ├─ 45-75 сек между волнами
Wave 2 (09:01):  3-5 сессий   ─┤
                                 ├─ имитирует новых пользователей
Wave 3 (09:02):  остаток      ─┘
```

**Эффект:** Выглядит как натуральный приток пользователей, не бот-шторм.

### 2. Ручные задержки и вариабельность

```python
# В каждой сессии добавляются случайные задержки:

# 1. Перед вводом текста (500-1500 мс)
await asyncio.sleep(random.uniform(0.5, 1.5))

# 2. При печати каждого символа (50-150 мс)
await page.keyboard.type(char)
await asyncio.sleep(random.uniform(0.05, 0.15))

# 3. Перед нажатием кнопок (500-1500 мс)
await asyncio.sleep(random.uniform(0.5, 1.5))

# Результат: Вместо 1 сек на ввод текста, нужно 5-10 сек
# = Выглядит как реальный человек печатает
```

### 3. Утаивание браузерных сигналов

```python
# Скрываем факт автоматизации
await context.new_context(
    user_agent='Mozilla/5.0 (Windows NT 10.0; Win64; x64) ...'  # Реальный UA
)

# Добавляем в скрипт страницы
await page.add_init_script("""
    Object.defineProperty(navigator, 'webdriver', {
        get: () => false,  // Скрываем что это автоматизация
    });
""")
```

### 4. Обработка "Allow" диалогов

Система автоматически нажимает кнопку Allow при появлении:

```python
# Каждую секунду проверяем наличие Allow
while True:
    allow_button = await page.query_selector('button:has-text("Allow")')
    if allow_button:
        await allow_button.click()  # Нажимаем как человек
    await asyncio.sleep(1)
```

---

## 🔐 Дополнительные Меры Защиты

### Уровень 1: Стандартный (Встроенный)

**Для 1-100 сессий**

```yaml
Что работает автоматически:
✅ Волнообразное распределение
✅ Случайные задержки  
✅ Утаивание WebDriver
✅ User-Agent ротация

Результат: 99% успеш, практически нет банов
```

### Уровень 2: Усиленный (Рекомендуется)

**Для 100-500 сессий**

1. **Используйте VPN или прокси**
   ```bash
   # Установите прокси серверы
   pip install aiohttp-socks
   ```

2. **Распределите нагрузку на несколько машин**
   ```
   Машина 1: 100 сессий на IP 1
   Машина 2: 100 сессий на IP 2
   Машина 3: 100 сессий на IP 3
   ```

3. **Используйте разные браузеры**
   ```python
   # Чередуйте браузеры
   browser_type = random.choice(['chromium', 'firefox'])
   browser = await playwright.browser_type(browser_type).launch()
   ```

### Уровень 3: Максимальный (Для 500+ сессий)

1. **Облако с rotатинг IP**
   - AWS Auto Scaling с разными VPC
   - Google Cloud с множеством регионов
   - Heroku + прокси сервис

2. **Residential прокси**
   ```python
   # Используйте реальные IP адреса
   proxy = "http://user:pass@proxy.residential.com:8080"
   context = await browser.new_context(proxy=proxy)
   ```

3. **Меньше одновременных сессий**
   ```
   Вместо: 500 сессий одновременно
   Используйте: 50 сессий × 10 дней
   ```

---

## ⚠️ Знаки, что вас банят

### Знак 1: Timeout ошибки

```
⏱️ Сессия 1: Timeout при загрузке
⏱️ Сессия 2: Timeout при загрузке
⏱️ Сессия 3: Timeout при загрузке
```

**Что делать:**
- Уменьшите количество сессий в волне
- Увеличьте задержку между волнами
- Добавьте VPN

### Знак 2: 403 Forbidden ошибки

```
❌ Сессия 5: 403 Forbidden
❌ Сессия 10: 403 Forbidden
```

**Что делать:**
- **СРОЧНО**: Остановите все задачи
- Выжидайте 1-2 часа перед новой попыткой
- Используйте другой IP адрес / VPN

### Знак 3: Быстрое истощение сессий

```
Должно быть: 5-10 минут на сессию
Происходит: 30 секунд на сессию
```

**Что делать:**
- Добавьте случайные задержки в код
- Проверьте не слишком ли много одновременных сессий
- Посмотрите логи для ошибок

---

## 📊 Рекомендуемые Параметры

### Маленькая операция (тестирование)

```
Сессий в волне: 2-3
Задержка между волнами: 60-90 сек
Всего сессий: 5-10
Время выполнения: 5-10 минут

Риск бана: 🟢 Минимальный

Используйте для:
- Тестирования кода
- Первых запусков
- Маленьких вопросов
```

### Средняя операция (продакшн)

```
Сессий в волне: 5-10
Задержка между волнами: 45-75 сек
Всего сессий: 50-150
Время выполнения: 1-2 часа

Риск бана: 🟡 Умеренный (требует внимания)

Используйте для:
- Обычных исследований
- Production запусков
- Средних объемов данных

Рекомендации:
- Добавьте VPN если возможно
- Мониторьте выполнение
- Имейте резервный IP
```

### Крупная операция (масштабирование)

```
Сессий в волне: 10-20+
Задержка между волнами: 30-60 сек
Всего сессий: 500-5000+
Время выполнения: 4-24 часа

Риск бана: 🔴 Высокий (требует опыта)

Используйте для:
- Крупных интеграций
- Масштабных исследований
- Долгоживущих задач

ОБЯЗАТЕЛЬНО:
- Используйте облачное решение
- Задействуйте residential прокси
- Будьте готовы к банам
- Распределите нагрузку в TIME
- Имейте backup план
```

---

## 🧪 Тестирование перед большим запуском

### Тест 1: Малый масштаб (5 сессий)

```
1. Запустите с 5 сессиями
2. Мониторьте каждую в реальном времени
3. Проверьте нет ли ошибок 403
4. Ждите полного завершения
```

Если успешно → переходите к шагу 2.

### Тест 2: Средний масштаб (25 сессий)

```  
1. Запустите с 25 сессиями
2. Мониторьте со своего ноутбука
3. Обратите внимание на скорость выполнения
4. Проверьте логи на странные паттерны
```

Если успешно → готовы к production.

### Тест 3: Production масштаб (100+ сессий)

```
1. Запустите на облачном сервере
2. Используйте VPN или прокси
3. Распределите по времени если возможно
4. Будьте готовы к быстрой остановке
```

---

## 💾 Сохранение результатов на случай бана

Если вас банят посередине, потечёте ваши результаты:

```python
# Результаты автоматически сохраняются каждые 10 сессий
task.save_result(session_id, success, data)

# Файл сохраняется в results/
# results/task_1000000_results.json
```

**Откуда восстановить:**

```bash
# Посмотрите что уже завершено
ls -lh results/

# Запустите оставшиеся сессии позже
# (они не будут дублировать уже завершенные)
```

---

## 🎯 Чек-лист перед большим запуском

Перед тем как запустить 500+ сессий:

- [ ] Я тестировал с 5 сессиями - ✅ прошло
- [ ] Я тестировал с 25 сессиями - ✅ прошло
- [ ] У меня есть VPN/прокси если нужно
- [ ] Я на облачном сервере (не на своем ноутбуке)
- [ ] Я прочитал все логи и понял как работает
- [ ] Я знаю как быстро остановить все сессии
- [ ] У меня есть резервный IP адрес
- [ ] Я готов к потенциальному бану
- [ ] Результаты будут сохранены в files
- [ ] Я мониторю выполнение первые 30 минут

---

## 📈 Мониторинг в реальном времени

### Что смотреть во время выполнения:

```bash
# Терминал 1: Логи приложения
tail -f gemini_automation.log

# Терминал 2: Использование системы
top

# Терминал 3: WebSocket события
# (в браузере F12 → Network → WS)
```

### Здоровые признаки:

```
✅ Progress растет плавно
✅ Нет 403 ошибок
✅ Нет ошибок Timeout
✅ Сессии занимают 5+ минут каждая
✅ CPU < 80%, RAM < 80%
```

### Тревожные признаки:

```
❌ 403 Forbidden ошибки
❌ Все сессии падают одновременно
❌ Progress застопорился на сессии X
❌ CPU/RAM на 100%
❌ Много timeout ошибок подряд
```

---

## 🆘 Что делать если вас забанили

### Не паникуйте! 

Баны обычно временные (1-24 часа).

### Шаг 1: Немедленно остановитесь

```bash
# Остановите все задачи через UI или API
curl -X POST http://localhost:8000/api/tasks/task_id/stop

# Закройте приложение
Ctrl+C
```

### Шаг 2: Спите

```
1-2 часа = обычный временный бан
4-6 часов = среднесрочный бан
24 часа = долгосрочный бан
```

### Шаг 3: Используйте другой IP

```bash
# Способ 1: Перезагрузите маршрутизатор
# (если у вас динамический IP)

# Способ 2: Используйте VPN
# Подключитесь к другому VPN серверу

# Способ 3: Используйте облако
# Запустите на другом облачном провайдере
```

### Шаг 4: Начните с малого

```
После разбана начните с 5 сессий
Если пройдет - 10 сессий
Если пройдет - 20 сессий
...
```

---

## 🔥 Best Practices

### ✅ ДЕЛАЙТЕ

1. **Увеличивайте постепенно**
   - 5 → 10 → 25 → 50 → 100

2. **Мониторьте постоянно**
   - Особенно первые 30 минут

3. **Используйте таймаут**
   - Не запускайте 1000 сессий в пятницу
   - Завершите в рабочее время

4. **Сохраняйте результаты**
   - Все результаты сохраняются автоматически

### ❌ НЕ ДЕЛАЙТЕ

1. **Не запускайте тонны сессий сразу**
   - 500 сессий без VPN = практически 100% бан

2. **Не используйте одинаковые задержки**
   - Системаиспользует random.uniform() для вариабельности

3. **Не прерывайте внезапно**
   - Используйте кнопку Stop, не Ctrl+C

4. **Не запускайте ночью**
   - Если что-то сломается, будет поздно

---

**Помните**: Лучше запустить 50 сессий успешно, чем 500 и получить бан.

**Safe automating! 🚀**
