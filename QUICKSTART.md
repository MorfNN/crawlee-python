# 🚀 Быстрый Старт - Gemini Automation

## ⚡ За 5 минут до первой задачи

### Шаг 1: Установка (2 минуты)

```bash
# Перейдите в директорию проекта
cd /workspaces/crawlee-python

# Установите зависимости
pip install -r requirements-automation.txt

# Установите Playwright браузеры
playwright install chromium
```

### Шаг 2: Запуск (1 минута)

```bash
# Способ 1: На Linux/Mac
bash run.sh

# Способ 2: На Windows или напрямую Python
python gemini_automation.py
```

Вы должны увидеть:
```
🚀 Запуск приложения Gemini Automation
Запуск на http://0.0.0.0:8000
```

### Шаг 3: Откройте веб-интерфейс (1 минута)

Откройте в браузере: **http://localhost:8000**

### Шаг 4: Создайте вашу первую задачу (1 минута)

1. Введите текст задачи: `"Сколько стоит акция NVIDIA?"`
2. Установите количество сессий: `5`
3. Нажмите **"Добавить задачу"**
4. Нажмите **"Запустить"** на созданной задаче

Готово! 🎉 Сессии начнут запускаться волнообразно.

---

## 📊 Что происходит?

```
Волна 1: 3 сессии запускаются  →→→ работают ~5 минут
   ↓ (ждем 45-75 сек)
Волна 2: 2 сессии запускаются  →→→ работают ~5 минут
   ↓
Готово!
```

---

## 🎯 Типичные сценарии использования

### Сценарий 1: Небольшой тест (10-20 сессий)

```
Задача: "Найди последние новости о Python 3.13"
Сессии: 15
Время выполнения: ~20 минут
Требования: Любой компьютер
```

### Сценарий 2: Среднее исследование (50-100 сессий)

```
Задача: "Сравни React и Vue.js"
Сессии: 75
Время выполнения: ~1 час
Требования: 8GB RAM, 2+ ядра
```

### Сценарий 3: Крупная кампания (200-500 сессий)

```
Задача: "Расширенное исследование тренда AI"
Сессии: 300
Время выполнения: ~2 часа
Требования: VPS/облако, 16GB+ RAM
```

---

## 💡 Советы и трюки

### Совет 1: Использование Ctrl+Enter для быстрого добавления

В текстовом поле задачи нажмите **Ctrl+Enter** - задача добавится автоматически.

### Совет 2: Запуск нескольких задач одновременно

Вы можете создавать и запускать несколько задач параллельно:

```
Задача 1: "Вопрос A" - 30 сессий ✅ Запущена
Задача 2: "Вопрос B" - 20 сессий ✅ Запущена  
Задача 3: "Вопрос C" - 50 сессий ✅ Запущена
```

Все будут выполняться одновременно!

### Совет 3: Мониторинг в реальном времени

Progress bar показывает ход выполнения. Обновления приходят каждые 2 секунды.

### Совет 4: Остановка опасность для расходов

Если что-то пошло не так, нажмите **"Остановить"** - все сессии закроются.

---

## 🔗 API для автоматизации

Если вы хотите интегрировать в свой код:

### Пример Python

```python
import asyncio
import aiohttp

async def create_and_run_task():
    async with aiohttp.ClientSession() as session:
        # Создаем задачу
        async with session.post('http://localhost:8000/api/tasks', 
            json={'prompt': 'Напиши стих о Python', 'sessions': 5}
        ) as resp:
            task = await resp.json()
            task_id = task['id']
        
        # Запускаем
        async with session.post(
            f'http://localhost:8000/api/tasks/{task_id}/start'
        ) as resp:
            await resp.json()
        
        # Мониторим
        while True:
            async with session.get(
                f'http://localhost:8000/api/tasks/{task_id}'
            ) as resp:
                task = await resp.json()
                print(f"Статус: {task['status']}")
                print(f"Завершено: {task['completed_sessions']}/{task['session_count']}")
                
                if task['status'] in ['completed', 'stopped']:
                    break
            
            await asyncio.sleep(2)

asyncio.run(create_and_run_task())
```

### Пример cURL

```bash
# Создать задачу
curl -X POST http://localhost:8000/api/tasks \
  -H "Content-Type: application/json" \
  -d '{"prompt": "Тестовая задача", "sessions": 3}'

# Получить статус
curl http://localhost:8000/api/tasks

# Запустить задачу
curl -X POST http://localhost:8000/api/tasks/task_1000000/start

# Остановить
curl -X POST http://localhost:8000/api/tasks/task_1000000/stop
```

---

## 📈 Мониторинг статистики

Если вы хотите посмотреть здоровье приложения:

```bash
curl http://localhost:8000/health
```

Ответ:
```json
{
  "status": "ok",
  "tasks": 2,
  "active_sessions": 3,
  "ws_connections": 1
}
```

---

## 🐛 Что если что-то не работает?

### Браузер не открывается

```bash
# Переустановите Playwright
playwright install chromium --with-deps
```

### Ошибка "Already listening on port 8000"

```bash
# Измените порт в коде или закройте процесс
lsof -ti:8000 | xargs kill -9
```

### Сессии падают с ошибкой timeout

- Проверьте интернет соединение
- Увеличьте timeout в коде (строка ~330 в gemini_automation.py)
- Запустите меньше сессий одновременно

---

## 📂 Где найти результаты?

Результаты сохраняются в папке `results/`:

```
results/
├── task_1000000_results.json
├── task_1000001_results.json
└── ...
```

Каждый файл содержит:
```json
{
  "task_id": "task_1000000",
  "prompt": "Сколько стоит акция NVIDIA?",
  "session_count": 5,
  "completed_sessions": 5,
  "failed_sessions": 0,
  "results": [...]
}
```

---

## 🚀 Продвинутые техники

### Техника 1: Пример использования через скрипт

```bash
python examples_api.py demo
```

Это запустит полную демонстрацию всех возможностей.

### Техника 2: Интеграция с вашу систему

Используйте REST API для интеграции в любую систему:

```javascript
// JavaScript пример
fetch('http://localhost:8000/api/tasks', {
  method: 'POST',
  headers: {'Content-Type': 'application/json'},
  body: JSON.stringify({
    prompt: 'Описание задачи',
    sessions: 10
  })
})
.then(r => r.json())
.then(task => console.log('Задача создана:', task.id))
```

---

## 📞 Поддержка

Если у вас есть вопросы:

1. Проверьте логи: `tail -f gemini_automation.log`
2. Посмотрите консоль браузера (F12)
3. Прочитайте [полную документацию](AUTOMATION_README.md)

---

## ✨ Что дальше?

После того как вы попробовали первую задачу:

- [ ] Запустить задачу с большим количеством сессий (50+)
- [ ] Создать несколько задач одновременно
- [ ] Интегрировать с вашей системой через API
- [ ] Настроить автоматическое запланирование задач

**Enjoy! 🚀**
